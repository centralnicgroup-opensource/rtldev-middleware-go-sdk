module github.com/centralnicgroup-opensource/rtldev-middleware-go-sdk/v5

go 1.26.0 // using this version to make it compatible with dnscontrol

require (
	github.com/stretchr/testify v1.12.1 // using this version to make it compatible with dnscontrol
	golang.org/x/net v0.59.0 // using this version to make it compatible with dnscontrol
	golang.org/x/text v0.42.0 // using this version to make it compatible with dnscontrol
)

require go.yaml.in/yaml/v3 v3.0.5 // indirect
